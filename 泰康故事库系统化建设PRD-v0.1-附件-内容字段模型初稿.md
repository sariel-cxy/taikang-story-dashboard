# 泰康故事库系统化建设 PRD v0.1 - 附件：内容字段模型初稿

日期：2026-05-14

## 附件说明

本附件用于配合《泰康故事库系统化建设 PRD v0.1》进行技术评估。字段来自当前静态原型和 Excel 数据结构，正式字段类型、是否必填、可见范围、字段级权限、导入映射方式需 RD 根据大内容平台能力确认。

## 1. 基础信息字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 编号 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 故事唯一编号 |
| 故事标题 | 待 RD 根据平台字段类型确认 | 是 | 按可见范围配置 | 当前列表和故事卡核心字段 |
| 故事摘要 | 待 RD 根据平台字段类型确认 | 是 | 按可见范围配置 | 可用于列表、卡片、汇报页 |
| 首次发布时间 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前故事卡已有字段 |
| 时间（仅开始年份） | 待 RD 根据平台字段类型确认 | 否 | 内部 | 用于时间统计 |
| 地点 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | 用于地点统计 |
| 细分地点 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 可作为详细地点 |

## 2. 标签字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 一级标签（战略概念） | 待 RD 根据平台字段类型确认 | 是 | 按可见范围配置 | 当前展示为“总领概念” |
| 二级标签（业务板块） | 待 RD 根据平台字段类型确认 | 是 | 按可见范围配置 | 如之家、人寿、公益等 |
| 三级标签（八大关系） | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前筛选维度 |
| 四级标签（叙事类型） | 待 RD 根据平台字段类型确认 | 否 | 内部 | 用于叙事分类 |
| 主角角色 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | 用于角色统计 |
| 协同角色 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前故事卡已有字段 |
| 人物主题标签 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前故事卡已有字段 |
| 价值观标签 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | 当前筛选维度 |
| 传播用途标签 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 用于传播复用 |

## 3. 内容详情字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 金句 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | 可用于传播引用 |
| 背景 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 可能包含内部判断 |
| 核心情节 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 故事主体内容 |
| 关键转折/细节 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前故事卡已有字段 |
| 结果/影响 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | 需审核后对外使用 |
| 价值意义/传播价值 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 传播判断字段 |
| 关联故事 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 后续可扩展为关联关系 |
| 原文 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 可能为敏感字段 |

## 4. 来源字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 来源标题 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前故事卡已有字段 |
| 来源类型 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前故事卡已有字段 |
| 来源链接 | 待 RD 根据平台字段类型确认 | 否 | 内部/受限 | 不建议默认公开 |
| 故事提供者 | 待 RD 根据平台字段类型确认 | 否 | 内部/受限 | 可能涉及内部人员或部门 |
| 关键人物名称 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | 涉及人物信息，需审核 |
| 备注 | 待 RD 根据平台字段类型确认 | 否 | 内部/受限 | 默认内部字段 |

## 5. 权限/状态字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 内容状态 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 草稿、待审核、审核通过、待发布、已发布、已撤回、已下架 |
| 审批状态 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 需与审批流联动 |
| 发布状态 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 按渠道可拆分 |
| 是否30周年重点故事 | 待 RD 根据平台字段类型确认 | 否 | 内部/汇报页 | 当前已有字段 |
| 重要程度 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 当前补标字段 |
| 是否适合对外传播 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 当前补标字段，可作为发布前判断 |
| 敏感等级 | 待 RD 根据平台字段类型确认 | 否 | 内部/受限 | 建议新增 |
| 可见范围 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 内部、部门、公开等 |

## 6. 可见范围/发布字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 适用展示范围 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 内部、公开、受限等 |
| 展示版本标题 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | P2 可扩展 |
| 展示版本摘要 | 待 RD 根据平台字段类型确认 | 否 | 按可见范围配置 | P2 可扩展 |
| 发布状态 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 可按展示范围分别记录 |
| 发布时间 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 发布记录 |
| 发布人 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 发布记录 |

## 7. 审计字段

| 字段名称 | 字段类型 | 是否必填 | 可见范围 | 备注 |
|---|---|---|---|---|
| 创建人 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 系统自动记录 |
| 创建时间 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 系统自动记录 |
| 最近修改人 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 系统自动记录 |
| 最近修改时间 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 系统自动记录 |
| 审核人 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 审批通过时记录 |
| 审核时间 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 审批通过时记录 |
| 审批意见 | 待 RD 根据平台字段类型确认 | 否 | 内部 | 退回或通过时记录 |
| 版本号 | 待 RD 根据平台字段类型确认 | 是 | 内部 | 用于版本追踪 |
| 操作日志 | 待 RD 根据平台字段类型确认 | 是 | 管理员/审计 | 可由平台统一日志承载 |
