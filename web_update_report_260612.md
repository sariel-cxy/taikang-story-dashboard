# 泰康故事库网页数据更新报告

## 1. 文件信息

| 项目 | 内容 |
|---|---|
| 仓库路径 | `/Users/sariel_cxy/Documents/GitHub/taikang-story-dashboard` |
| Excel 主表路径 | `/Users/sariel_cxy/Desktop/codex/taikang_new/泰康故事库主表_260612.xlsx` |
| Excel sheet 名称 | `故事库主表` |
| Excel 故事总数 | 117 |
| Excel 编号范围 | `TK-00001` 至 `TK-00117` |
| 更新后网页故事总数 | 117 |

## 2. 编号变化

| 检查项 | 结果 |
|---|---|
| 新增编号 | 无 |
| 删除编号 | 无 |
| 保留编号 | 117 个 |
| 重复编号 | 无 |
| 空编号 | 无 |
| 异常编号 | 无，全部符合 `TK-xxxxx` 格式 |
| Excel 与网页编号顺序 | 完全一致 |

本次为全量替换。当前网页原有编号集合与新 Excel 相同，但所有故事字段均重新以 Excel 为唯一数据源生成，没有从旧网页数据补写内容。

## 3. 字段映射

| Excel 字段名 | story-data.js 字段名 | 是否成功映射 | 备注 |
|---|---|---|---|
| 编号 | 编号 | 是 | 直接映射 |
| 故事标题 | 故事标题 | 是 | 直接映射 |
| 故事摘要 | 故事摘要 | 是 | 直接映射 |
| 一级标签（总领概念） | 一级标签（战略概念） | 是 | 兼容网页现有字段名 |
| 二级标签（业务板块） | 二级标签（业务板块） | 是 | 直接映射 |
| 场景标签 | 场景标签 | 是 | 直接映射 |
| 三级标签（八大关系） | 三级标签（八大关系） | 是 | 直接映射 |
| 四级标签（叙事类型） | 四级标签（叙事类型） | 是 | 直接映射 |
| 主角角色 | 主角角色 | 是 | 直接映射 |
| 协同角色 | 协同角色 | 是 | 直接映射 |
| 关键人物名称 | 关键人物名称 | 是 | 直接映射 |
| 人物主题标签 | 人物主题标签 | 是 | 直接映射 |
| 价值观标签 | 价值观标签 | 是 | 直接映射 |
| 重要程度 | 重要程度 | 是 | 直接映射 |
| 是否30周年重点故事 | 是否30周年重点故事 | 是 | 直接映射 |
| 是否适合对外传播 | 是否适合对外传播 | 是 | 直接映射 |
| 传播用途标签 | 传播用途标签 | 是 | 直接映射 |
| 时间（仅开始年份） | 时间（仅开始年份） | 是 | 直接映射 |
| 地点 | 地点 | 是 | 直接映射 |
| 细分地点 | 细分地点 | 是 | 直接映射 |
| 金句 | 金句 | 是 | 对应需求中的“一句话金句” |
| 背景 | 背景 | 是 | 直接映射 |
| 核心情节 | 核心情节 | 是 | 直接映射 |
| 关键转折/细节 | 关键转折/细节 | 是 | 直接映射 |
| 结果/影响 | 结果/影响 | 是 | 直接映射 |
| 价值意义/传播价值 | 价值意义/传播价值 | 是 | 直接映射 |
| 原文 | 原文 | 是 | 直接映射 |
| 关联故事 | 关联故事 | 是 | 直接映射 |
| 来源标题 | 来源标题 | 是 | 直接映射 |
| 来源类型 | 来源类型 | 是 | 直接映射 |
| 来源链接 | 来源链接 | 是 | 直接映射 |
| 故事提供者 | 故事提供者 | 是 | 直接映射 |
| 首次发布时间 | 首次发布时间 | 是 | 直接映射 |
| 备注 | 备注 | 是 | 直接映射 |

网页现有的 `__isKeyStory` 和 `__searchText` 为兼容页面筛选、搜索逻辑生成的派生字段，不是额外故事内容。筛选选项、统计图数据和元数据已依据新 Excel 全量重建。

## 4. 修改文件清单

本次实际修改：

- `data/story-data.js`
- `story-dashboard/data/story-data.js`
- `web_update_report_260612.md`（本报告，新文件）

未修改 `index.html`、页面结构、样式、筛选逻辑或其他网页文件。未复制 Excel 到仓库。

## 5. 检查结果

| 检查项 | 结果 |
|---|---|
| JS 语法检查 | 通过，两份文件均通过 `node --check` |
| JS 数据加载 | 通过，两份文件均可在 Node VM 中生成 `window.STORY_DATA` |
| 两份 story-data.js 是否一致 | 是，字节完全一致 |
| 故事总数是否一致 | 是，Excel 与两份网页数据均为 117 条 |
| 编号是否一致 | 是，编号集合及顺序完全一致 |
| Excel 逐字段回读比对 | 通过，34 个 Excel 字段无内容差异 |
| 旧编号残留 | 无 |
| 网页缺失的 Excel 编号 | 无 |
| 重复、空或异常编号 | 无 |
| 字段空值异常 | 未发现；Excel 空单元格按现有结构保留为空字符串 |
| 格式或转义异常 | 未发现；引号、换行和特殊字符经 JSON 转义后可正常解析 |
| 页面引用 | 根目录及 `story-dashboard` 镜像目录分别引用对应的 `data/story-data.js`，未发现其他数据入口 |
| 浏览器视觉检查 | 未执行图形化浏览器检查；静态引用、JS 语法和数据加载检查均通过 |

## 6. git 状态

`git status --short`（生成报告后预期状态；原有未跟踪文件继续保留且未处理）：

```text
 M data/story-data.js
 M story-dashboard/data/story-data.js
?? data/story-data.js.bak_260524_20260524_201837
?? story-dashboard/data/story-data.js.bak_260524_20260524_201837
?? web_update_report.md
?? web_update_report_260612.md
?? 泰康故事库字段与标签库_RD查看版_v0.2.xlsx
?? 泰康故事库字段与标签库整理_给IT-RD.xlsx
?? 泰康故事库字段字典_给IT-RD.csv
?? 泰康故事库接入大内容平台IT对接材料.md
?? 泰康故事库标签库_给IT-RD.csv
?? 泰康故事库系统化建设PRD-v0.1-附件-内容字段模型初稿.md
```

`git diff --stat`：

```text
 data/story-data.js                 | 743 +++++++++++++++++++------------------
 story-dashboard/data/story-data.js | 743 +++++++++++++++++++------------------
 2 files changed, 744 insertions(+), 742 deletions(-)
```

## 7. 复核结论

网页静态数据已使用 `泰康故事库主表_260612.xlsx` 全量重新生成。两份数据文件内容一致，故事总数、编号顺序和 34 个源字段均与 Excel 完全一致，JS 语法及数据加载检查通过。

当前可以进入人工复核阶段。尚未执行 `git add`、`git commit` 或 `git push`。
